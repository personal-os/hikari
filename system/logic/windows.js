
///////////// Wallpaper Switcher

function openWallpaperFolder() {
	$("#folder").dialog({
		title: "Wallpaper Switcher",
		minimize: "#desktop-taskbar"
	});
}

////////////////////////// Chips

function openChipFolder() {
	$("#chip-folder").dialog({
		title: "Chip Folder",
		minimize: "#desktop-taskbar"
	});
}

/////////////////////// SubChips

function openSubchipFolder() {
	$("#subchip-folder").dialog({
		title: "SubChip",
		minimize: "#desktop-taskbar"
	});
}

/////////////////// Data Library

function openDataLibFolder() {
	$("#datalib-folder").dialog({
		title: "Data Library",
		minimize: "#desktop-taskbar"
	});
}

//////////////////////// NetNavi

function openNetNaviFolder() {
	$("#netnavi-folder").dialog({
		title: "NetNavi",
		minimize: "#desktop-taskbar"
	});
}

////////////////////////// Email

function openEmailFolder() {
	$("#email-folder").dialog({
		title: "E-mail",
		minimize: "#desktop-taskbar"
	});
}

////////////////////// Key Items

function openKeyItemFolder() {
	$("#keyitem-folder").dialog({
		title: "Key Item",
		minimize: "#desktop-taskbar"
	});
}

//////////////////////// Network

function openNetworkFolder() {
	$("#network-folder").dialog({
		title: "Network",
		minimize: "#desktop-taskbar"
	});
}

/////////////////////////// Maps

function openMapFolder() {
	$("#map-folder").dialog({
		title: "Map",
		minimize: "#desktop-taskbar"
	});
}
